{{- if .Values.cronJob.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "devices-api.fullname" . }}-load-dd
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "devices-api.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cronJob.schedule | quote}}
  concurrencyPolicy: "Forbid"
  jobTemplate:
    spec:
      template:
        metadata:
          annotations:
          {{- with .Values.podAnnotations }}
            {{- toYaml . | nindent 8 }}
          {{- end }}
          labels:
            {{- include "devices-api.selectorLabels" . | nindent 12 }}
        spec:
          initContainers:
          - name: edmunds-vehicles-sync
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            command: ['sh', '-c', "/devices-api edmunds-vehicles-sync"]
            envFrom:
            - configMapRef:
                name: {{ include "devices-api.fullname" . }}-config
            - secretRef:
                name: {{ include "devices-api.fullname" . }}-secret
          - name: parkers-vehicles-sync
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            command: ['sh', '-c', "/devices-api parkers-vehicles-sync"]
            envFrom:
            - configMapRef:
                name: {{ include "devices-api.fullname" . }}-config
            - secretRef:
                name: {{ include "devices-api.fullname" . }}-secret
          - name: search-sync-dds
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            command: ['sh', '-c', "/devices-api search-sync-dds"]
            envFrom:
            - configMapRef:
                name: {{ include "devices-api.fullname" . }}-config
            - secretRef:
                name: {{ include "devices-api.fullname" . }}-secret
          containers:
          - name: completed
            image: busybox
            command: ['sh', '-c', 'echo "edmunds-vehicles-sync and search-sync-dds completed"']
          restartPolicy: OnFailure
{{ end }}          